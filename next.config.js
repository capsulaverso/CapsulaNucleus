// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any Next.js specific configurations here.
  // For example, image domains, experimental features, etc.
};

module.exports = nextConfig;
